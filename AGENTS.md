# ChatDomePro — Agent 工作规范

## Agent skills

### Issue tracker

工作项与规格保存在 ChatDomePro 的 GitHub Issues 中。操作规则见 `docs/agents/issue-tracker.md`。

### Triage labels

Issue 使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix` 表示处理状态。映射规则见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文模型。领域词汇保存在根目录 `CONTEXT.md`，架构决策记录保存在 `docs/adr/`。读取规则见 `docs/agents/domain.md`。
